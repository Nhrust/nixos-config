# =============================================================================
# conf/profile/desktop.conf — Поведение, специфичное для десктопа
# =============================================================================
# У десктопа нет крышки и батареи. Файл оставлен пустым для единообразия —
# hyprland.conf источит conf/profile.conf одинаково независимо от профиля.
# =============================================================================
