# =============================================================================
# conf/profile/server.conf — Поведение, специфичное для сервера
# =============================================================================
# Сервер — машина для непрерывной работы. hypridle здесь НЕ запускается,
# экран не гаснет автоматически, сессия не блокируется по таймауту.
# Для ручной блокировки используй Super+L (бинд из binds.conf).
#
# Файл оставлен пустым для единообразия — hyprland.conf источит
# conf/profile.conf одинаково независимо от профиля.
# =============================================================================
